# API 할당량 초과 문제 진단 보고서

## 문제 상황
서버 로그에서 "Api token quota exceeded" 오류가 지속적으로 발생하고 있습니다.

## 사용 중인 API 목록

### 1. 건강보험심사평가원 (HIRA) API
**서비스 키**: `api_key` 환경변수 사용

#### 1-1. 병원 목록 API
- **엔드포인트**: `/B551182/hospInfoServicev2/getHospBasisList`
- **용도**: 병원 검색 및 목록 조회
- **사용 빈도**: 높음 (지역별, 종별 필터링 시마다 호출)
- **페이지네이션**: 최대 50페이지 (5000개 병원)

#### 1-2. 비급여 가격 API
- **엔드포인트**: `/B551182/nonPaymentDamtInfoService/getNonPaymentItemHospDtlList`
- **용도**: 병원별 비급여 항목 가격 조회
- **사용 빈도**: 중간 (비교 페이지에서 선택된 병원별로 호출)
- **페이지네이션**: 최대 10페이지 (1000개 항목)

### 2. 행정안전부 API
**서비스 키**: `api_key` 환경변수 사용 (동일한 키)

#### 2-1. 행정구역 코드 API
- **엔드포인트**: `/1741000/StanReginCd/getStanReginCdList`
- **용도**: 시도/시군구 목록 조회
- **사용 빈도**: 낮음 (초기 로드 시 1회, 캐싱됨)
- **페이지네이션**: 전체 데이터 수집 (약 20,555개 레코드)

## 할당량 증설 필요 API

### 우선순위 1: HIRA 병원 목록 API
- **이유**: 가장 빈번하게 호출됨
- **호출 패턴**: 
  - 지역별 검색 시마다 호출
  - 종별 필터링 시마다 호출
  - 페이지네이션으로 최대 50페이지까지 호출 가능
- **예상 호출량**: 사용자 1명이 서울 전체 검색 시 약 50회 호출 (페이지네이션)

### 우선순위 2: HIRA 비급여 가격 API
- **이유**: 비교 기능에서 필수
- **호출 패턴**: 선택된 병원 수만큼 호출 (최대 5개 병원)
- **예상 호출량**: 사용자 1명이 5개 병원 비교 시 5회 호출

### 우선순위 3: 행정안전부 지역 코드 API
- **이유**: 초기 로드 시 1회만 호출되고 캐싱됨
- **호출 패턴**: 서버 시작 시 또는 캐시 만료 시 (12시간)
- **예상 호출량**: 하루 최대 2회

## 증설 권장 사항

1. **HIRA API 할당량**: 일일 최소 10,000회 이상 권장
2. **행정안전부 API 할당량**: 일일 최소 100회 이상 권장 (캐싱으로 충분)

## 확인 방법

공공데이터 포털 (https://www.data.go.kr)에서:
1. 내 API 관리 → 사용량 통계 확인
2. 각 API별 일일 호출 횟수 확인
3. 할당량 초과된 API 확인
4. 필요 시 할당량 증설 신청

## 증설 후 확인 사항

### 증설 반영 확인
- 증설 신청 후 즉시 반영되는 경우도 있지만, 보통 몇 시간~하루 정도 소요될 수 있음
- 증설된 서비스 키가 현재 사용 중인 `api_key`와 동일한지 확인

### 증설 완료 후 테스트
1. 실제 API 데이터 반환 확인
2. 페이지네이션 작동 확인 (최대 50페이지, 5000개 병원)
3. 전국 시도별 데이터 수집 확인
4. 종별 필터링 작동 확인

## 현재 상태 (최종 확인)

- **HIRA 병원 목록 API**: ❌ 여전히 할당량 초과 (2025-11-14 확인)
  - **운영계정 신청 완료**: 사용자 확인 완료
  - **가능한 원인**: 
    1. 운영계정 서비스 키와 개발계정 서비스 키가 다를 수 있음
    2. 현재 사용 중인 서비스 키가 개발계정 키일 수 있음
    3. 운영계정 서비스 키로 업데이트 필요할 수 있음
- **행정안전부 지역 코드 API**: ✅ 정상 작동
- **HIRA 비급여 가격 API**: ✅ 정상 작동

## 운영계정 서비스 키 확인 필요

운영계정으로 신청 완료되었다면:
1. 공공데이터 포털에서 운영계정 서비스 키 확인
   - https://www.data.go.kr/iim/api/selectAPIAcountView.do
   - "병원정보서비스" API의 운영계정 서비스 키 복사
2. 현재 사용 중인 서비스 키와 비교
3. 운영계정 서비스 키로 업데이트 필요 시:
   - `backend/.env` 파일의 `api_key` 값 업데이트
   - EC2 서버의 `.env` 파일도 동일하게 업데이트
   - PM2 재시작
