> ⚠️ **아카이브(2026-07)**: AWS EC2 운영 당시(2025-11-13)의 특정 시점 스냅샷입니다.
> 백엔드 인프라는 이후 Render로 전환되었습니다. 현재 상태는
> [`doc/RENDER_INFO.md`](../RENDER_INFO.md)를 참고하세요.

# 배포 상태 요약

**최종 업데이트**: 2025-11-13

## ✅ 완료된 작업

1. **EC2 서버 배포 완료**
   - 인스턴스: `i-030a6f1fd19110d16` (boam79-sever1)
   - 퍼블릭 IP: `54.180.251.93`
   - Node.js v20.19.5, PM2 6.0.13 설치 완료
   - 서버 프로세스 실행 중 (PM2)

2. **포트 변경 완료**
   - 백엔드 포트: 3001 → **3000**으로 변경
   - 보안 그룹에 포트 3000이 이미 열려있음 (사용자 확인)
   - `.env` 파일 및 서버 코드 업데이트 완료
   - EC2 서버 재배포 완료

3. **서버 상태 확인**
   - ✅ PM2 프로세스 정상 실행 중
   - ✅ 로컬 Health Check 정상 응답: `{"status":"ok","timestamp":"..."}`
   - ⚠️ 외부 접근 테스트 필요 (보안 그룹 확인)

## 🔍 현재 상태

### 서버 내부 테스트
```bash
# EC2 서버 내부에서 테스트 (성공)
curl http://localhost:3000/health
# 응답: {"status":"ok","timestamp":"2025-11-13T04:22:32.841Z"}
```

### 외부 접근 테스트
```bash
# 로컬에서 테스트 (타임아웃)
curl http://54.180.251.93:3000/health
# 상태: 연결 타임아웃
```

## ⚠️ 확인 필요 사항

### 보안 그룹 설정
사용자가 확인한 보안 그룹 포트:
- ✅ 포트 22 (SSH)
- ✅ 포트 80 (HTTP)
- ✅ 포트 3000 (백엔드)
- ✅ 포트 8000

**하지만 외부 접근이 안 되는 경우:**
1. 보안 그룹 규칙 확인
   - 소스 IP가 `0.0.0.0/0`인지 확인
   - 규칙이 실제로 적용되었는지 확인
2. EC2 인스턴스 방화벽 확인
   - Ubuntu 방화벽 (ufw) 상태 확인
   - 필요시 `sudo ufw allow 3000` 실행

## 📋 다음 단계

1. **보안 그룹 재확인**
   - AWS 콘솔에서 보안 그룹 규칙 확인
   - 포트 3000의 소스 IP가 `0.0.0.0/0`인지 확인

2. **EC2 방화벽 확인**
   ```bash
   ssh -i ../boam79-aws-key.pem ubuntu@54.180.251.93
   sudo ufw status
   sudo ufw allow 3000  # 필요시
   ```

3. **외부 접근 테스트**
   ```bash
   curl http://54.180.251.93:3000/health
   ```

4. **프론트엔드 연결**
   - 프론트엔드 환경변수 설정: `NEXT_PUBLIC_API_BASE_URL=http://54.180.251.93:3000/opendata`
   - CORS 설정 확인 (백엔드에서 프론트엔드 URL 허용)

## 📝 참고

- 서버는 정상 작동 중이므로 프론트엔드 개발은 계속 진행 가능
- Mock 데이터로 정상 작동 중
- 실제 공공데이터 API는 500/401 에러 발생 (서비스 키 확인 필요)

