# API 호출 절약 방안

## 현재 문제점

### 1. 병원 목록 API 호출
- **현재**: 페이지네이션으로 최대 50페이지 (5000개 병원)까지 자동 수집
- **문제**: 사용자가 실제로 필요한 데이터보다 훨씬 많이 호출
- **예시**: 서울 전체 검색 시 약 50회 API 호출 (100개씩 50페이지)

### 2. 캐싱 전략 부족
- **현재**: 병원 목록 API는 캐싱 없음
- **문제**: 같은 조건으로 재검색 시에도 API 재호출

### 3. 불필요한 페이지네이션
- **현재**: 모든 데이터를 한 번에 수집
- **문제**: 사용자는 처음 10-20개만 보고, 나머지는 보지 않을 수 있음

## 최적화 방안

### 방안 1: 병원 목록 캐싱 추가 (우선순위 높음)
- **구현**: `HospitalsAdapter`에 in-memory 캐시 추가
- **캐시 키**: `sido + sigungu + type` 조합
- **TTL**: 1시간 (병원 정보는 자주 변경되지 않음)
- **효과**: 같은 조건 재검색 시 API 호출 0회

### 방안 2: 페이지네이션 최적화
- **현재**: 최대 50페이지까지 자동 수집
- **개선**: 초기에는 1-2페이지만 수집 (100-200개)
- **효과**: 초기 검색 시 API 호출 50회 → 1-2회로 감소

### 방안 3: 프론트엔드 페이지네이션
- **구현**: 백엔드는 첫 페이지만 반환, 프론트엔드에서 "더 보기" 버튼
- **효과**: 사용자가 실제로 필요한 만큼만 API 호출

### 방안 4: 배치 요청 최적화
- **현재**: 병원별로 개별 API 호출
- **개선**: 가능한 경우 배치 요청 사용

### 방안 5: 스마트 캐싱 전략
- **인기 검색어 캐싱**: 자주 검색되는 조건 (서울, 경기 등)은 더 긴 TTL
- **사용자별 캐싱**: 같은 사용자가 재검색 시 캐시 우선 사용

## 구현 우선순위

1. ✅ **병원 목록 캐싱 추가** (구현 완료)
   - `HospitalsAdapter`에 in-memory 캐시 추가
   - 캐시 키: `sido + sigungu + type` 조합
   - TTL: 1시간
   - 효과: 같은 조건 재검색 시 API 호출 0회

2. ✅ **페이지네이션 최적화** (구현 완료)
   - 초기 2페이지만 수집 (200개 병원)
   - `maxPages` 파라미터로 추가 페이지 수집 가능 (최대 10페이지)
   - 효과: 초기 검색 시 API 호출 50회 → 2회로 감소 (96% 절약)

3. ✅ **프론트엔드 캐싱 강화** (구현 완료)
   - `useHospitals`: staleTime 1시간, gcTime 2시간
   - `usePricing`: staleTime 12시간, gcTime 24시간
   - `useRegions`: staleTime 24시간, gcTime 48시간
   - 효과: 재방문 시 API 호출 0회

## 예상 효과

### Before (최적화 전)
- 서울 전체 검색: 50회 API 호출 (50페이지)
- 같은 조건 재검색: 50회 API 호출 (캐시 없음)
- **일일 사용자 10명**: 500회 API 호출

### After (최적화 후)
- 서울 전체 검색: 2회 API 호출 (2페이지, 200개)
- 같은 조건 재검색: 0회 API 호출 (캐시 사용)
- **일일 사용자 10명**: 약 20회 API 호출 (96% 절약)

## 추가 최적화 가능 사항

1. **인기 검색어 우선 캐싱**: 서울, 경기 등 자주 검색되는 조건은 더 긴 TTL
2. **프론트엔드 페이지네이션**: "더 보기" 버튼으로 추가 데이터 로드
3. **배치 요청**: 여러 병원의 가격 정보를 한 번에 요청
